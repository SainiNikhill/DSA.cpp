#include<iostream>
#include<stack>
using namespace std;

int main()
{
    stack<int> st;// creation of stack 

    //insertion can be done using push operation
    st.push(3);
    st.push(5);
    st.push(7);


    // deletion can be done using pop operation
   st.pop();

}